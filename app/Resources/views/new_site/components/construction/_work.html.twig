<div class="work">
    <div class="work__image">
        <img src="{{ asset(constructionWork.path) }}" alt="">
    </div>

    <div class="work__text">
        <h4>{{ constructionWork.title }}</h4>
        <p>
            {{ constructionWork.description | striptags | slice(0,75) }}...
        </p>
        <a href="{{ path('construction_work', {id: constructionWork.id, work: constructionWork.title}) }}"
           class="btn btn--dark btn--small">Ver mas</a>
    </div>
</div>