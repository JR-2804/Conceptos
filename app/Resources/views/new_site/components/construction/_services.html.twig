<section class="section services">
    <div class="section__title services__title">
        <h2>Servicios realizados</h2>
    </div>

    <div class="services__list">
        {% for i in 1..5 %}
            {{ include('new_site/components/construction/_service.html.twig') }}
        {% endfor %}
    </div>
</section>